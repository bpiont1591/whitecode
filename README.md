# whitecode

System opini został usunięty z tej wersji strony.

## Co jest wyłączone

- formularz i widok opini na stronie,
- endpoint `POST /review`,
- endpoint `GET /reviews`,
- endpoint `GET /db/init` dla opini.

## Obecnie aktywne

- formularz kontaktowy z logowaniem Discord,
- endpointy auth (`/auth/*`),
- endpoint kontaktu (`POST /contact`).
