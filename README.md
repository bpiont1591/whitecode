# whitecode

System opini został całkowicie usunięty z projektu.

Aktualnie strona działa bez opinii i bez endpointów/bazy dla opinii.
Pozostawione są tylko:
- logowanie Discord (`/auth/*`),
- formularz kontaktowy (`POST /contact`).
